import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })
const signatureFont = "YourSignatureFontVariable" // added signature font variable to root layout

export const metadata: Metadata = {
  title: "Madhusudan | Portfolio",
  description: "Full Stack Developer & Data Analyst Portfolio",
  generator: "v0.app",
  icons: {
    icon: [
      {
        url: "/icon.svg",
        type: "image/svg+xml",
      },
    ],
    apple: "/icon.svg",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`font-sans antialiased bg-background text-foreground scroll-smooth ${signatureFont}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
